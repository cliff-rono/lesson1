import 'package:flutter/material.dart';

class Marianne extends StatelessWidget {
  const Marianne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
